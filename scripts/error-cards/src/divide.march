mod RefineErr2 do
  needs IO.Console

  fn safe_div(n : Int, d : {Int | _ != 0}) : Int do
    n / d
  end

  fn main(_cap_console : Cap(IO.Console)) do
    println(int_to_string(safe_div(100, 0)))
  end

end
