mod CapErr do
  needs IO.Console
  needs IO.Random

  fn make_token() : String do
    Bytes.to_string(random_bytes(16))
  end

  fn issue() : String do
    make_token()
  end

  fn main() do
    println(issue())
  end

end
