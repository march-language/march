mod CapErr do
  needs IO.Console
  needs IO.Random

  fn make_token() : String do
    Bytes.to_string(random_bytes(16))
  end

  fn issue() : String do
    make_token()
  end

  fn main(_cap_console : Cap(IO.Console), _cap_random : Cap(IO.Random)) do
    println(issue())
  end

end
