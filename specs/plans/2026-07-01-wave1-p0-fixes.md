# Wave 1 P0 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix the 14 remaining P0 bugs from `specs/analysis/2026-07-01-pipeline-deep-review.md` §2 (items 4–17), one commit per fix, each with a failing-test-first regression test.

**Architecture:** Surgical point fixes across three regions — `lib/tir/lower.ml` (match compilation), `lib/tir/llvm_emit.ml` (codegen), and the front end (`lib/lexer/lexer.mll`, `lib/parser/token_filter.ml`, `lib/parser/parser.mly`, `lib/desugar/desugar.ml`). No refactors in this wave; refactors are Wave 3.

**Tech Stack:** OCaml 5.3 / dune / menhir / ocamllex; alcotest suites in `test/`; March-language repro programs compiled via the built compiler.

## Global Constraints

- **Worktree:** work ONLY in `/Users/80197052/code/march/.claude/worktrees/hopeful-kapitsa-9f49f3` on branch `claude/hopeful-kapitsa-9f49f3`. Never touch the main repo checkout.
- **Off-limits files:** `lib/tir/perceus.ml`, `lib/tir/borrow.ml`, `test/test_properties.ml` — a concurrent session owns them. If your fix seems to require editing them, STOP and report BLOCKED.
- **Build/test:** `dune build` and `scripts/run-tests.sh` (full, ~17s) / `scripts/run-tests.sh -q` (quick, ~2s). If `dune` is not on PATH, prefix with `/Users/80197052/.opam/march/bin/`. NEVER use `eval $(opam env ...)`. Judge test runs by **exit code**, not tail output.
- **TDD:** write the failing test first, run it to see it fail (proving it exercises the bug), then fix, then see it pass. For compiled-only bugs the red test must run the **compiled** binary, not just the interpreter.
- **Run the FULL suite (`scripts/run-tests.sh`, exit 0) before every commit.**
- **Commits:** stage files explicitly by name (`git add path1 path2`); NEVER `git add -A`/`.`/`-am`. No `Co-Authored-By` lines. Message format: `fix(<area>): <what>` referencing the finding id, e.g. `fix(lower): panic on guard exhaustion instead of returning 0 (B3)`.
- **Decision graph:** before your first edit run `deciduous add action "<your task title>" -c 85` then `deciduous link 1420 <new_node_id> -r "Wave 1 task"`. After committing: `deciduous add outcome "<result>" -c 90 --commit HEAD` and link it to your action node.
- **Compiler invocation for repros:** `./_build/default/bin/main.exe --compile -o <out> <file.march>` (compiled) and `./_build/default/bin/main.exe <file.march>` (interpreter). Put repro files in the session scratchpad or `/tmp/wave1-kapitsa-*.march` (suffix to avoid cross-session collisions). CAS cache auto-invalidates on compiler/runtime changes.
- **March syntax for repro programs:** `mod Name do ... end`; `if c do ... else ... end` (else REQUIRED, no `then`); lambdas `fn x -> body` (arrow only); match arms may be `|`-led; no `;`. See `syntax_reference.md`. Do not name anything `init`.
- **Benchmarks:** tasks marked [RC-adjacent] must run `dune exec march -- bench/tree_transform.march`, `bench/list_ops.march`, `bench/binary_trees.march` after the fix and compare against `specs/benchmarks.md`; report numbers.
- **Reference:** finding details with evidence are in `specs/analysis/2026-07-01-pipeline-deep-review.md` (B-ids below match it). Line numbers were verified 2026-07-01 at commit 78e31ff7; re-verify before editing since earlier tasks may shift them.
- **Test suite homes:** compile-and-run regression tests follow existing patterns in `test/test_codegen.ml` (IR + run tests; see its compile helpers) or `test/native/` fixtures (`.march` + `.expected` + dune rule — grep `test/dune` for an existing fixture name to copy the rule pattern). Interpreter/parser tests: `test/test_compiler.ml`, `test/test_eval.ml`. Match the file's existing alcotest group style.

---

### Task 1: B3 — guard exhaustion must panic, not return 0

**Files:**
- Modify: `lib/tir/lower.ml:1173` and `lib/tir/lower.ml:1119`
- Test: `test/test_codegen.ml` (compiled run test)

**Interfaces:** none consumed/produced; behavior-only.

Context: `compile_matrix_impl` has a `nonexhaustive_panic` helper (defined near lower.ml:1028, comment explains why returning `LitInt 0` is wrong). But the guarded-match fallthrough at :1173 and the empty-branches case at :1119 still emit `Tir.EAtom (Tir.ALit (Ast.LitInt 0))` with a `(* match failure *)` comment. A guarded match where every guard fails returns `0` reinterpreted at arbitrary type.

- [ ] **Step 1: Write the failing test.** Repro program:

```march
mod GuardEx do
  fn classify(n) do
    match n do
      x when x > 0 -> "pos"
      x when x < 0 -> "neg"
    end
  end

  fn main() do
    println(classify(0))
  end
end
```

Add a compiled-mode test asserting the binary exits non-zero with a non-exhaustive-match panic message (match how existing panic-path tests assert — grep test_codegen.ml for `non-exhaustive` / `panic`). Also assert interpreter behavior agrees (run the same file through the interpreter; if the interpreter currently panics, parity; if it also returns garbage, cover both and note it in your report).

- [ ] **Step 2: Run the new test — expect FAIL** (compiled binary prints garbage/empty instead of panicking).
- [ ] **Step 3: Fix.** Hoist `nonexhaustive_panic` so it is in scope at both sites (it currently lives inside `compile_matrix_impl`); replace both `LitInt 0` fallbacks with it. Preserve any span/message detail the helper takes.
- [ ] **Step 4: Run the new test — expect PASS. Run full suite — exit 0.** If existing tests relied on the silent-0 behavior, examine each: they are latent wrong-code tests; fix the test programs to be exhaustive and say so in your report.
- [ ] **Step 5: Commit** `fix(lower): panic on guard exhaustion instead of returning 0 (B3)` — stage `lib/tir/lower.ml`, the test file(s), nothing else.

---

### Task 2: B4 — float-literal match arms are silently dropped; unhandled pattern rows must fail loudly

**Files:**
- Modify: `lib/tir/lower.ml` (`pat_tag_and_subs` :925–947; grouping loop `| None -> ()` :1089–1091)
- Likely modify: `lib/tir/llvm_emit.ml` (`emit_case` — string-chain region near :4616–4638) and possibly `lib/tir/js_emit.ml` for parity
- Test: `test/test_codegen.ml`

**Interfaces:** produces a new `br_tag` encoding for float literals — document the chosen encoding in a comment at `pat_tag_and_subs` AND at the `emit_case` decoder.

Context: `pat_tag_and_subs` returns `None` for `Ast.LitFloat` (and `PatRecord`); `is_trivial_pat` is also false for them; the grouping loop discards such rows via `| None -> () (* trivial — should not appear here *)`. Interpreter (eval.ml, AST-level) matches floats fine → interpreter/compiled divergence. Same shape as the fixed PatAtom bug 99e5cf82 (regression test at test/test_eval.ml:731–743). `PatRecord` is currently unreachable from the grammar (no `{...}` pattern production), so for records fail-loudly is sufficient — do NOT implement record-pattern compilation (YAGNI).

- [ ] **Step 1: Write the failing test.** Repro (use `|`-led arms — newline-led float arms don't parse until Task 11):

```march
mod FloatPat do
  fn name(x) do
    match x do
      1.5 -> "one-and-a-half" | 2.5 -> "two-and-a-half" | _ -> "other"
    end
  end

  fn main() do
    println(name(2.5))
  end
end
```

(If single-line pipe-separated arms don't parse, use `| 2.5 ->` leading-pipe layout — verify against the grammar; the review confirmed pipe-led float arms parse.) Test asserts compiled output == interpreter output == `two-and-a-half`.

- [ ] **Step 2: Run it — expect FAIL** (compiled prints `other`).
- [ ] **Step 3: Fix.**
  1. Add a `LitFloat` case to `pat_tag_and_subs`, mirroring how `LitString` is encoded into `br_tag` (strings use a quoted encoding; floats need an unambiguous encoding in the same namespace — the `br_tag` namespace table is documented in the analysis doc §9.3). Ensure the tag string round-trips exactly (use `Float.to_string`-style canonical form consistently at encode and decode sites).
  2. In `llvm_emit.ml`'s `emit_case`, add a float-chain analogous to the string chain (`fcmp oeq double` comparisons instead of `march_string_eq` calls). Check whether the scrutinee needs a coerce to `double` first (the string chain has a known missing-coerce issue — don't replicate it).
  3. Check `lib/tir/js_emit.ml` for a case decoder; if it decodes string tags, add float support there too (or `failwith` loudly — JS backend parity noted in report).
  4. Replace the grouping loop's `| None -> ()` with `failwith` naming the unhandled pattern kind and its span, and make `pat_tag_and_subs` explicitly `failwith "record patterns not yet compilable"` for `PatRecord` rather than returning `None`.
- [ ] **Step 4: New test PASSES; full suite exit 0.** Also test a float arm with NO wildcard falling through to the (Task 1) panic.
- [ ] **Step 5: Commit** `fix(lower,llvm): compile float-literal match arms; fail loudly on unhandled pattern rows (B4)`.

---

### Task 3: B9 — `march_` prefix check off-by-one (dead codegen arm)

**Files:**
- Modify: `lib/tir/llvm_emit.ml:1433–1434`
- Test: `test/test_codegen.ml` (IR-level), investigation notes in report

**Interfaces:** none.

Context: `String.sub n 0 7 = "march_"` compares a 7-char substring to a 6-char literal — always false. The three sibling checks use `0 6` (llvm_emit.ml:575, :3243, :3481). The arm exists for C-runtime externs used as first-class values; with it dead, TFn-typed values get a closure trampoline and erased values get **called with 0 args**.

- [ ] **Step 1:** Fix the substring length to 6.
- [ ] **Step 2: Build; run FULL suite.** The arm becoming live is a behavior change — if any test fails, investigate each failure (the arm may now fire where the fallback used to); report what changed. Do NOT blind-revert; if semantics are unclear, report BLOCKED with the failing cases.
- [ ] **Step 3: Regression coverage.** Find a surface-March program whose lowering reaches `emit_atom` with a `march_`-prefixed name (candidate: a runtime comparator/HOF flow; grep lower.ml/mono.ml output paths for where `march_*` names become atoms). If reachable: add an `ir_contains` test asserting the bare-address form is emitted. If NOT reachable from surface syntax, say so explicitly in your report and add an OCaml-level comment at the fixed line noting the guard's tested-by status.
- [ ] **Step 4: sort_by investigation (bounded, ≤15 min).** The still-open `List.sort_by` 100+ element compiled crash may intersect this arm (runtime fns passed to HOFs). Run a 150-element `List.sort_by` program compiled, before/after your fix. Document the result either way in your report — do not scope-creep into fixing it.
- [ ] **Step 5: Commit** `fix(llvm): repair off-by-one march_ prefix guard; runtime externs as values (B9)`.

---

### Task 4: B10 — RC-op skips must respect local shadowing [RC-adjacent]

**Files:**
- Modify: `lib/tir/llvm_emit.ml:4091–4133` (five arms: EIncRC/EDecRC/EFree and atomic variants)
- Test: `test/test_codegen.ml`

**Interfaces:** none.

Context: the five RC-op arms skip emission when the atom's name is in `top_fns` or `is_builtin_fn`, with no `var_slot` check. `emit_atom` itself has the correct guard in two places — :1363–1371 (top-fns arm) and :1440–1452 (builtin arm, whose comment cites heap corruption). A local heap value named `link`, `send`, `monitor`, or shadowing any top-level fn gets zero RC ops.

- [ ] **Step 1: Write the failing test.** Compile this and assert the emitted IR for `main` contains a `march_decrc` (or `march_free`) for the local — inspect the IR before the fix to pick a robust assertion; the point is: before fix, ZERO RC ops reference the shadow's slot; after, ≥1:

```march
mod ShadowRc do
  fn main() do
    let link = String.concat("heap", "-allocated")
    let out = String.concat(link, "!")
    println(out)
  end
end
```

(`link` is a runtime/builtin name; adjust the shadow name to whichever of `link`/`send`/`monitor` is actually in `is_builtin_fn` — check the list at :431–562.)

- [ ] **Step 2: Run — expect FAIL** (no RC op emitted for the shadowed local).
- [ ] **Step 3: Fix.** Add `not (Hashtbl.mem ctx.var_slot <name>)` to all five skip conditions, exactly mirroring the emit_atom guards.
- [ ] **Step 4: Test passes; full suite exit 0; run the three RC benchmarks** and compare to `specs/benchmarks.md` (this only ADDS RC ops for shadowed names, so no regression expected — confirm).
- [ ] **Step 5: Commit** `fix(llvm): RC ops must not be skipped for locals shadowing builtins/top-fns (B10)`.

---

### Task 5: B5 — EUpdate on type-erased records writes past the allocation

**Files:**
- Modify: `lib/tir/llvm_emit.ml:4214–4251` (EUpdate arm)
- Possibly create: `march_record_update_dyn` in `runtime/march_runtime.c` + preamble declare in llvm_emit
- Test: `test/test_codegen.ml`

**Interfaces:** if the dyn path is built, its C signature must mirror `march_record_field_dyn`'s conventions (study that function first — runtime/march_runtime.c; it's the model).

Context: when `get_record_fields` returns `[]` (type-erased base), `emit_heap_alloc ctx 0 0` allocates a header-only cell, then the update loop writes every field at offset 16 via the `field_index_for` fallback `(0, TVar "_")` (:1686–1693). `EField` at :4192–4205 handles the same situation with a `march_record_field_dyn` runtime fallback; `EUpdate` has no counterpart.

- [ ] **Step 1: Find the trigger.** Write a March program that routes a record through a type-erasing flow (e.g. a generic function whose parameter type is a bare type variable) and performs a record-update on it. Consult `syntax_reference.md` for record-update syntax. Verify with `--compile` that it reaches the `all_fields = []` path (add a temporary `Printf.eprintf` if needed; remove before commit). If you cannot construct a surface-language trigger after a genuine attempt (~20 min), the fail-loudly fix below is still required; document non-reachability in the report.
- [ ] **Step 2: Failing test.** If triggerable: compiled output must equal interpreter output (currently: corruption/garbage). If not triggerable: an expect-test that the compiler emits a clear error, not silent bad IR.
- [ ] **Step 3: Fix — two-stage policy.** (a) Insert a loud guard: when `all_fields = []` and the update list is non-empty, `failwith "EUpdate on type-erased record — march_record_update_dyn required (B5)"`. Run the full suite. (b) **If** the guard trips for any existing test/stdlib path OR your Step-1 trigger works, implement the real dyn path: a `march_record_update_dyn` runtime function modeled on `march_record_field_dyn` (copy the source record's fields into a fresh cell, overwrite the named fields), declare it in the preamble, and call it from the EUpdate arm in the erased case. If the guard never trips and there's no surface trigger, ship stage (a) only.
- [ ] **Step 4: Full suite exit 0.** If you touched `runtime/*.c`: CAS invalidates automatically; still re-run one compiled test to confirm the fresh runtime linked.
- [ ] **Step 5: Commit** `fix(llvm): EUpdate on type-erased records — dyn path / loud failure instead of OOB writes (B5)`.

---

### Task 6: B7 — mutual-TCO back-edge drops the Perceus dec-chain [RC-adjacent]

**Files:**
- Modify: `lib/tir/llvm_emit.ml` (mutual-TCO arm region :2969–3002; model arms at :2301–2339 (ELet) and :2351 (ESeq))
- Test: `test/test_codegen.ml`

**Interfaces:** none.

Context: self-TCO has a dedicated arm intercepting `ELet(tmp, EApp(self, args), dec_chain)` that emits the DecRC/Free chain BEFORE the back-edge. The mutual-TCO arm has no ELet/ESeq counterparts: the generic ELet handler emits the back-edge, opens a dead block (:2997), and the dec-chain lands in the dead block. `tail_calls_in` (:5002–5005) and `has_non_tail_group_call` (:5025–5032) prove this shape is expected in mutual groups. Result: leaked cells every iteration of a Perceus-wrapped mutual tail loop.

- [ ] **Step 1: Write the failing test.** Mutual tail recursion where each step allocates (so Perceus wraps the tail call with a dec-chain). Sketch (adapt list-cons syntax to March's actual syntax — check `syntax_reference.md`; `Cons(n, xs)` works if `[h | t]` doesn't):

```march
mod MutualLeak do
  fn ping(xs, n) do
    if n == 0 do List.length(xs) else pong(Cons(n, xs), n - 1) end
  end

  fn pong(xs, n) do
    if n == 0 do List.length(xs) else ping(Cons(n, xs), n - 1) end
  end

  fn main() do
    println(to_string(ping(Nil, 500000)))
  end
end
```

First confirm the shape actually triggers mutual TCO AND carries a dec-chain: inspect the pre-emit TIR (`--dump-phases` via forge, or the debug flag the codebase provides) and the emitted IR — before the fix, the dec-chain instructions must sit in a block with no predecessors. Then make the leak observable: run the compiled binary under `/usr/bin/time -l` and assert max RSS < a threshold that the leaking version exceeds (leak of one cons cell × 500k iterations ≫ 10 MB). Alternatively (preferred if simpler): an IR-structural assertion that no `march_decrc` lands in an unreachable block. Pick whichever is robust; show it failing.

- [ ] **Step 2: Run — expect FAIL.**
- [ ] **Step 3: Fix.** Add mutual-group twins of the self-TCO interception arms: `ELet(tmp, EApp(group_member, args), dec_chain)` and the ESeq variant — emit the dec-chain, then the back-edge, mirroring :2301–2339's ordering exactly (including any stacksave handling the self path does).
- [ ] **Step 4: Test passes; full suite exit 0; run the three RC benchmarks**, compare to `specs/benchmarks.md`, report numbers.
- [ ] **Step 5: Commit** `fix(llvm): emit Perceus dec-chain before mutual-TCO back-edge (B7)`.

---

### Task 7: B8 — mutual-TCO loop has no reduction check

**Files:**
- Modify: `lib/tir/llvm_emit.ml` (`emit_mutual_tco_group` :5350–5542)
- Test: `test/test_codegen.ml` (IR-level)

**Interfaces:** none.

Context: `emit_fn`'s TCO path calls `emit_reduction_check` at the top of every loop iteration (:5266–5269) and at entry for non-leaf functions (:5301). `emit_mutual_tco_group` never calls it — a pure mutually-recursive loop starves its scheduler worker.

- [ ] **Step 1: Failing test:** compile a mutual-recursion program (reuse Task 6's shape or a simpler `is_even`/`is_odd`) and assert via `ir_contains` that the combined mutual function's loop body contains the reduction-check call (find the exact callee name `emit_reduction_check` emits by reading it). Expect FAIL.
- [ ] **Step 2: Fix:** insert `emit_reduction_check ctx` immediately after the `mutual_loop` label, mirroring how :5266–5269 places it relative to stacksave/tag-load.
- [ ] **Step 3: Test passes; full suite exit 0.** (Scheduler-starvation behavioral test is out of scope — IR assertion suffices; note that in the report.)
- [ ] **Step 4: Commit** `fix(llvm): reduction check in mutual-TCO loops prevents scheduler starvation (B8)`.

---

### Task 8: B12 — `cur_type_defs` global goes stale for REPL/fragment emitters

**Files:**
- Modify: `lib/tir/llvm_emit.ml` (`cur_type_defs` ref at :205; single assignment in `emit_module` :6011; all readers — grep `!cur_type_defs`; REPL/fragment entry points :6659, :6707, :6748, :6781, :6849)
- Test: existing JIT parity tests + one new REPL test

**Interfaces:** `ctx` gains a `type_defs` field — later tasks (9) may rely on it; name it `ctx.type_defs`.

Context: every niche/newtype representation decision (EAlloc :3651, EReuse :3831, emit_case :4287–4327, ensure_adt_eq_fn :1786) consults `!cur_type_defs`, which only `emit_module` sets. A JIT fragment can box an Option a previous fragment niche-encoded — ABI mismatch across `.so` boundaries (same family as the fixed cross-module Option repr bug).

- [ ] **Step 1: Failing test (best effort).** REPL/JIT test (pattern: clang-gated REPL tests in test_codegen.ml around :404–420): fragment 1 defines a niche-eligible ADT (`type Opt = Some(Int) | None`-alike) and a fn producing `Some`; fragment 2 matches on the value. If the harness can't express cross-fragment ADT flow, document why and rely on the Slow JIT parity tests as the gate.
- [ ] **Step 2: Fix.** Add `type_defs` to the `ctx` record; populate it in `make_ctx` (or at each ctx construction site) from the `types` parameter every entry point already receives; replace every `!cur_type_defs` read with `ctx.type_defs`; delete the global ref and its assignment. Purely mechanical — the compiler will find every site once the ref is gone.
- [ ] **Step 3: Full suite INCLUDING Slow tests** (`scripts/run-tests.sh`, not `-q`) — the 6 JIT/interpreter parity tests are the real gate here. Exit 0.
- [ ] **Step 4: Commit** `fix(llvm): thread type defs through ctx; REPL fragments no longer see stale representations (B12)`.

---

### Task 9: B11 — REPL closure wrapper diverged from the uniform ptr ABI

**Files:**
- Modify: `lib/tir/llvm_emit.ml:6803–6826` (`emit_repl_fn_with_closure_slot`'s inline wrapper; canonical implementation `clo_wrap_define` :401–422)
- Test: clang-gated REPL test in `test/test_codegen.ml`

**Interfaces:** consumes `ctx.type_defs` from Task 8 if relevant; otherwise none.

Context: commit e709bee9 made `clo_wrap_define` always return `ptr` (tagging scalars `(n<<1)|1`) because ECallPtr closure dispatch reads results as `ptr` (:3596–3604). The REPL emitter builds its own wrapper inline that still returns the raw concrete type; its comment falsely claims it is "identical" to the canonical one. A REPL-stored closure returning Int gets conditionally untagged: odd results halved.

- [ ] **Step 1: Failing test:** REPL test (follow the clang-gated pattern): store a closure returning an odd Int (e.g. define `fn mk() do fn -> 5 end end`, bind `let f = mk()`, call `f()`), assert the result is `5`. Expect FAIL (2) before the fix — if the harness can't reproduce the exact store-then-call path, verify against a manual `dune exec march --` REPL session and document; the IR diff (wrapper returns `ptr` + tag sequence) is the fallback assertion.
- [ ] **Step 2: Fix:** delete the inline wrapper; call `clo_wrap_define` (adapting its inputs — it may need the target fn name/arity/ret type which the REPL site already has). The stale "identical to lines 1095–1112" comment goes away with the deletion.
- [ ] **Step 3: Full suite incl. Slow; exit 0.**
- [ ] **Step 4: Commit** `fix(llvm): REPL closure wrapper uses canonical clo_wrap_define ptr ABI (B11)`.

---

### Task 10: B15 — raw newline in string literals desyncs all subsequent line numbers

**Files:**
- Modify: `lib/lexer/lexer.mll` (`read_string` :204–226 and `read_string_interp`; model: triple-string rule at :238 which calls `Lexing.new_line` correctly)
- Test: `test/test_compiler.ml`

**Interfaces:** none. Decision locked in: **keep accepting** raw newlines (rejecting would break existing multi-line strings); just track them.

- [ ] **Step 1: Failing test:** parse a module where a plain `"..."` string contains a raw newline, followed by a declaration; assert the following declaration's span has the correct line number (off by one before the fix). Follow test_compiler.ml's existing span-assertion style.
- [ ] **Step 2: Fix:** add a `'\n'` case to `read_string` (before the catch-all `_ as c`) that calls `Lexing.new_line lexbuf` and appends the char, matching the triple-string rule's handling. Do the same in `read_string_interp`.
- [ ] **Step 3: Test passes; full suite exit 0.**
- [ ] **Step 4: Commit** `fix(lexer): track raw newlines inside string literals; line numbers no longer desync (B15)`.

---

### Task 11: FLOAT missing from token filter's pattern-start set

**Files:**
- Modify: `lib/parser/token_filter.ml:115–123` (`is_pattern_start`)
- Test: `test/test_compiler.ml`

**Interfaces:** none.

- [ ] **Step 1: Failing test:** parse

```march
mod FloatArms do
  fn name(x) do
    match x do
      1.5 -> "a"
      2.5 -> "b"
      _ -> "c"
    end
  end
end
```

Expect FAIL before the fix (“expecting `end`” at `2.5`).

- [ ] **Step 2: Fix:** add `| Parser.FLOAT _ -> true` to `is_pattern_start`. Then audit the predicate against the grammar's `simple_pattern` rule (parser.mly:1289–1308) — the grammar rule is the source of truth. If other pattern-start tokens are missing (e.g. CHAR if it exists), add them too and cover each with one test arm.
- [ ] **Step 3: Tests pass; full suite exit 0.**
- [ ] **Step 4: Commit** `fix(parser): newline-led float (and audited) literal match arms parse (token filter)`.

---

### Task 12: B6 — pipe into parenthesized match silently discards the scrutinee

**Files:**
- Modify: `lib/desugar/desugar.ml:451–454` (EMatch case) and :436–444 (sibling ECond case using bare `failwith`)
- Test: `test/test_eval.ml` or `test/test_compiler.ml` (diagnostic assertion)

**Interfaces:** none.

Context: `x |> (match scrut do ...)` desugars by throwing away `scrut` and matching on `x` — verified silent wrong code. The sibling ECond case raises a bare `failwith` (uncaught `Failure` in some entry points) instead of a positioned diagnostic.

- [ ] **Step 1: Survey.** Grep stdlib/, test/, bench/, examples for `|>` followed by `match` or `(match` — if any legitimate uses exist, list them in your report before choosing behavior (expected: zero; they'd be bugs).
- [ ] **Step 2: Failing test:** `1 |> (match 2 do 1 -> "one" | 2 -> "two" | _ -> "x" end)` must produce a **compile-time diagnostic** (message should name the problem: "piping into a match discards its scrutinee; write `match x do ... end` instead"), not run. Assert via the diagnostics-capture pattern existing desugar/typecheck error tests use. Expect FAIL (currently compiles and prints "one").
- [ ] **Step 3: Fix:** replace the scrutinee-discarding EMatch case with a positioned error through the `Errors` diagnostic machinery (find how desugar reports other user-facing errors — if desugar currently has no diagnostic channel and only `failwith`, raise the same positioned exception type the parse path uses so bin/main.ml renders it with a span). Convert the ECond sibling to the same mechanism.
- [ ] **Step 4: Tests pass; full suite exit 0.**
- [ ] **Step 5: Commit** `fix(desugar): error on pipe-into-match instead of silently discarding scrutinee (B6)`.

---

### Task 13: B14 — interleaved same-name fn clause groups silently shadow

**Files:**
- Modify: `lib/parser/parser.mly:33–77` (`group_fn_clauses`)
- Test: `test/test_compiler.ml`

**Interfaces:** none.

Context: `group_fn_clauses` merges only ADJACENT same-name clauses; a comment promises a later validation pass that doesn't exist. `fn f(0) ... / fn other() ... / fn f(n) ...` compiles with the first group silently dead.

- [ ] **Step 1: Failing test:** the exact repro above must produce a compile error naming `f` and both locations; a program with properly adjacent multi-head clauses must still compile. Expect FAIL (compiles today).
- [ ] **Step 2: Fix.** After grouping, walk the decl list: collect each merged fn group's grouping key (read the code — key is likely name, possibly name+arity; use the SAME key the merger uses so the check matches merge semantics exactly). On a repeated key in non-adjacent groups at the same level, raise the positioned parse/validation error type this file already uses, with the second group's span and a message like "clauses of `f` must be adjacent; earlier clauses at line N would be silently unreachable". Mind: if the key is name-only but different-arity same-name fns are legal as separate functions, key on name+arity and note the decision in the report.
- [ ] **Step 3: Tests pass (error case + still-works adjacent case); full suite exit 0** — if any existing stdlib/test file has interleaved clauses, the suite will find them; fix those files (they harbor dead code today) and list them in the report.
- [ ] **Step 4: Commit** `fix(parser): reject interleaved same-name fn clause groups (B14)`.

---

### Task 14: B16 — remove the ~H CSRF `conn` auto-injection

**Files:**
- Modify: `lib/desugar/desugar.ml:269–347` (the `<form method=...>` scan and `CSRF.tag_string`/`conn` splice at :331)
- Test: `test/test_eval.ml` or codegen run test

**Interfaces:** none.

Context: every `~H` literal containing a mutating form gets an injected call `CSRF.tag_string(conn)` where `conn` is a free variable assumed in scope — breaks all non-Bastion `~H` users with baffling unbound-name errors. Bastion is being removed from the stdlib (project decision), so the injection is dead weight. **Decision locked in: remove the auto-injection entirely** (keep unrelated ~H processing: escaping, span-uniqueness, etc.).

- [ ] **Step 1: Survey.** Grep stdlib/, test/, and bench/ for `tag_string`, `csrf`, and `~H` templates with `method=` forms. If any in-tree test depends on the auto-injection, list them — they get updated in this task (explicit `${CSRF.tag_string(conn)}` interpolation if the module genuinely wants it, or dropped if bastion-bound).
- [ ] **Step 2: Failing test:** a plain module with `fn page() do ~H"<form method=\"post\">x</form>" end` must compile and run standalone. Expect FAIL today ("I cannot find `conn`").
- [ ] **Step 3: Fix:** delete the injection branch (the form-method scan and splice); keep everything else. Update any Step-1 dependents.
- [ ] **Step 4: Tests pass; full suite exit 0.**
- [ ] **Step 5: Commit** `fix(desugar): remove ~H CSRF conn auto-injection (B16, bastion removal)`.

---

### Task 15: Specs bookkeeping

**Files:**
- Modify: `specs/todos.md` (Done section), `specs/progress.md` (Current State counts + capability notes)

**Interfaces:** none.

- [ ] **Step 1:** Run the full suite once; record the test count and any known-failure changes.
- [ ] **Step 2:** Append to `specs/todos.md` Done: one line per landed fix (B3, B4, B9, B10, B5, B7, B8, B12, B11, B15, FLOAT-filter, B6, B14, B16), referencing `specs/analysis/2026-07-01-pipeline-deep-review.md`. Update `specs/progress.md` Current State (test count, known failures) and add a bullet noting Wave 1 of the pipeline deep review landed. These files are append-only historical records — do not rewrite existing entries.
- [ ] **Step 3: Commit** `docs(specs): record Wave 1 P0 fixes in todos/progress`.
