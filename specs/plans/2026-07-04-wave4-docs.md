# Wave 4: Invariant Documentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Write down the load-bearing invariants of the March compiler that three waves of fixes kept rediscovering — completing §9 of `specs/analysis/2026-07-01-pipeline-deep-review.md` and, with it, the entire deep-review program.

**Architecture:** Prose documents that LIFT from the now-existing module doc comments (llvm_ctx's tag law, Tir_names' contracts, Rc_types' divergence table, the perceus env tiers) into narrative form, plus two carried-over syntax-doc corrections. Docs cite the owning module as the source of truth; the doc narrates, the module doc governs.

**Tech Stack:** Markdown; the live compiler at HEAD for claim verification; `scripts/check-docs.sh` for pointer guarding.

## Global Constraints — THE CLAIM-VERIFICATION GATE (this wave's discipline)

1. **Every semantic assertion is executed before it is written.** A claim like "even ints ≥ 4096 in natural repr would misclassify as heap pointers" gets a repro snippet compiled/run against `./_build/default/bin/main.exe` (or an IR inspection via `--emit-llvm`) and the report shows the verification transcript per claim, numbered. Claims that cannot be executed (design rationale, history) are cited to a commit or an existing test instead. NO unverified semantic claims — docs rot invisibly precisely because nobody executes them (the if/else/then case below is Exhibit A).
2. **Cite the owning module.** Where an invariant lives in a module doc comment (post-Wave-3 this is most of them), the document points there and summarizes — it does not fork the truth. Duplicated normative text must say which copy governs.
3. **doc-lint:** every new doc's compiler-source pointers must survive `scripts/check-docs.sh` (add the new files to its guard set if the script enumerates files — check how it discovers targets).
4. **Build/test:** `dune build --root .`; the six runners (isolated HOME: `mkdir -p .w4home && HOME=$PWD/.w4home ./_build/default/test/<t>.exe -e`) required ONLY for tasks that touch code (Task 4 potentially; Task 3's runtime-header comment is comment-only but still build-verified). `scripts/run-tests.sh` also works (fixed in 4c1eea4f).
5. **Process:** foreground only; wedge rule; no `git stash`; never pipe `march --compile` output; explicit staging; no Co-Authored-By; deciduous action linked from goal 1651 before edits, outcome `--commit HEAD` after; one commit per task.
6. **Worktree:** ONLY /Users/80197052/code/march/.claude/worktrees/hopeful-kapitsa-9f49f3 (branch claude/hopeful-kapitsa-9f49f3, base 94df6bb4 == main == origin/main). Do not touch test/test_properties.ml.
7. **Known pre-existing failures (NOT yours):** bench/heapsort.march RC underflow; examples/supervision_linear_drop.march race; mono "Monomorphization limit reached" (documented skip).
8. **Report-from-tree:** claims get diffed/verified by reviewers.

---

### Task 1: `docs/value-representation.md`

**Files:** Create `docs/value-representation.md`; possibly one-line pointer additions to `docs/runtime.md` (which currently has NONE of this).

Sections (each with verified claims or module citations): the `march_hdr` layout (cite runtime/march_runtime.h + llvm_ctx's layout constants); the tagged-immediate scheme and the conditional-untag law verbatim from `Llvm_ctx.emit_untag_scalar`'s doc (cite it as governing); UNIFORM vs NATURAL slot classification (tuples/erased cells/generic ADT fields vs records/concrete fields — verify with 2 `--emit-llvm` probes); closure struct layout + the uniform-ptr apply-wrapper ABI (cite Tir_names.is_apply_fn's doc + llvm_calls.clo_wrap_define); trampoline double-tagging 4n+3 (cite the task_await fix 291f6b5f + verify with a probe); atom hashing bit63==bit62; niche/newtype representation rules (cite Repr + the erased-Option convention notes); the shallow-free RC contract (who dups children — cite march_record_update_dyn's design + the EUpdate history). End with a "history" appendix: one line per bug this doc would have prevented (B5, B9, B11, task_await double-tag, cross-module Option repr) with commits.

- [ ] Verify every executable claim (numbered transcript in report); write; doc-lint; commit `docs(repr): value-representation contract — tagging law, slots, ABI, RC shallow-free (W4.1)`.

### Task 2: `specs/perceus-invariants.md`

**Files:** Create `specs/perceus-invariants.md`.

Sections: the owned/borrowed contract at call boundaries — what each promises, who decs, when — including the dual-position invariant (a var at owned AND borrowed positions must be dup'd; cite the B1 fix a5dad194 and its tests); the needs_rc / borrow_eligible divergence (SUMMARIZE + cite rc_types.ml as governing — do not fork the table); the closure-FV rule (captures are borrowing dups; locally-invoked closures don't transfer FV ownership; `$clo` at apply arg 0 always consumed — cite the fix lineage); FBIP preconditions (constructor FIELD count not type params; `$fbip$` markers; runtime rc==1 check means size is the only static obligation — cite the B2 fix); **the scrutinee-borrowed approximation REWRITTEN for post-campaign truth**: it is path-insensitive by design, the double-dec fix (20d1d144) excludes the scrutinee from cross-branch decs, and the OLD "protects sort_by" rationale is obsolete (sort_by was exonerated — the real bug was mono; cite the diagnosis). The env's three scoping tiers (cite perceus.ml's env doc as governing). Verification: MARCH_DEBUG_PERCEUS transcripts for 2 contract claims + the snapshot corpus as standing evidence (cite specific .expected files per claim where they pin it).

- [ ] Verify; write; doc-lint; commit `docs(perceus): ownership invariants — call boundaries, FBIP, scrutinee approximation post-campaign (W4.2)`.

### Task 3: TIR invariants + synthetic-name registry + actor runtime mirror comment

**Files:** Create `specs/features/tir-invariants.md`; comment-only edit to the runtime header where actor state words a[2..4] are read (find it: grep runtime/ for the actor dispatch state access).

Doc sections: ANF discipline (what may appear where — verify with a snapshot dump citation); `TVar "_"` semantics; the `ELetRec([fn], EAtom(AVar fn))` lambda pattern defun matches (cite defun + Tir_names.apply docs); the complete `br_tag` namespace table — ctors, `$TupleN`, decimal ints, quoted strings, `:atoms`, bools-in-either-case, `#`-hex floats (cite Tir_names + pat_tag_and_subs as governing; verify the float row with a probe); the explicit statement that TIR carries no spans (and the consequence: location-free diagnostics downstream — cite the analysis-doc finding as the accepted state). Synthetic-name registry: the `$`-family is collision-proof and governed by tir_names.ml (point); the `__`-family (`__arg0..N`, `__celem`, `__app_init__`, `__try_call*`, `__march_test_N__`) is LEXABLE and capturable in principle — table with producer, consumer, capturability status (verify capturability of ONE with a probe program — the front-end review's method). Runtime mirror comment: at the C site reading actor words a[2..4], add the comment cross-referencing Tir_names' `$d_/$e_/$f_` sort-prefix contract (comment-only; build + one compiled actor test to confirm untouched behavior).

- [ ] Verify; write + comment edit; doc-lint; six runners (comment touched runtime/ — prove no-op); commit `docs(tir): post-lowering invariants, br_tag namespace, synthetic-name registry; actor layout mirror comment (W4.3)`.

### Task 4: the if/else/then correction — survey-first, remove-if-unused default

**Files:** Modify `syntax_reference.md`, `CLAUDE.md`; POSSIBLY `lib/parser/parser.mly` + tests (only under the removal outcome).

The verified facts (front-end review, Wave 1): (a) `if c do ... end` WITHOUT else is a hard parse error — both docs say else is optional: FALSE; (b) `if c then e1 else e2` parses and runs — both docs say there is no `then` keyword: FALSE. The project's stated design (both docs + the da7ce42b then-specific-error investment) says `then` should not exist.

- [ ] **Survey:** grep ALL March sources in-tree (stdlib/, test/, bench/, examples/) + any doc snippets for the `then`-expression form. Re-verify facts (a) and (b) against HEAD first — three waves touched the parser; the truth may have moved.
- [ ] **If zero uses and (b) still true — the DEFAULT: remove the `then` production** (parser.mly), keeping/adding the targeted "March uses do...end, not then" error message (da7ce42b's machinery); add a parse-error test for the then-form; full six runners. **If uses exist or removal breaks anything non-trivially: STOP, report BLOCKED with the survey — the controller escalates to the user.**
- [ ] **Either way:** correct BOTH docs' `if` sections to the verified truth (else mandatory; then rejected-with-targeted-error OR exists-but-deprecated per outcome); doc-lint.
- [ ] Commit `fix(parser,docs): if/else is mandatory and then-form removed; syntax docs corrected to verified behavior (W4.4)` (adjust message to the outcome).

### Task 5: user-facing semantics notes

**Files:** Modify `syntax_reference.md` (a "Semantics notes" section) or create `docs/semantics-notes.md` — pick whichever the existing doc structure suggests; justify.

Items (EACH verified with a fresh probe at HEAD — several were found in Wave 1 and the compiler has changed since): top-level `let` RHS re-evaluated once per referencing function (compiled) vs once (interpreter); the newline-glom continuation rule (a line starting with `(`, `-`, or any infix operator continues the previous expression); derived `Ord`/`Hash` ignore constructor payloads (verify — was it fixed since?); nested-module default-arg fns drop default VALUES; the reserved soft-keyword asymmetry (`let init = 3` parses, `init + 1` doesn't — verify current state; W1 Task 11 changed pattern-position handling only). Items verified FIXED since Wave 1 get documented as current behavior instead — the probe decides.

- [ ] Verify all; write; doc-lint; commit `docs(syntax): verified semantics notes — top-level lets, newline continuation, derive caveats, soft keywords (W4.5)`.

### Task 6: bookkeeping — program closeout

**Files:** `specs/todos.md`, `specs/progress.md`, `specs/analysis/2026-07-01-pipeline-deep-review.md` gets a STATUS HEADER (allowed exception to historical-corpus immutability: a dated addendum block at the top marking §2/§7/§8/§9 dispositions with commit ranges — append-style, no rewriting).

- [ ] Six runners final counts; doc-lint; specs entries: Wave 4 complete, deep-review §9 done, THE PROGRAM COMPLETE (waves 1-4, with the one-paragraph campaign summary + total counts); the analysis-doc status header. Commit `docs(specs): Wave 4 complete — deep-review program closed out (W4.6)`.
