# Session types: Moved

This topic is now part of the **March Language Reference**. The canonical version is **[../lang/session-types.md](../lang/session-types.md)**; see the reference umbrella **[specs/lang/index.md](../lang/index.md)**. The implementation-detail sections this doc once contained (internal `session_ty`/`TChan` representation, `project_protocol`/`dual_session_ty` typing judgments, `VChan` evaluation, TIR lowering) are compiler-internal (bucket C) material slated for the sibling implementation reference (`specs/impl/index.md`, Task 5), not the language chapter; the full prior text remains in git history.
