- ✅ **JsonStream phase 2 Component 2b — `EvNumRaw(String)` opt-in lossless number mode (2026-07-31, `stdlib/json_stream.march`, `test/stdlib/test_json_stream.march`).** Added `EvNumRaw(String)` to `Event` (after `EvNum(Float)`) and `with_raw_numbers(st : JsState) : JsState`, a setter that flips a fourth `JsCfg` field (`raw`) rather than adding four more `start*` constructors — composes with every existing constructor (`start`/`start_with`/`start_ndjson`/`start_ndjson_with`). `EvNum(Float)` stays the default: phase 1's whole suite and the `Json.parse` differential both depend on it, so raw mode is strictly opt-in. `num_finalize` gained a `cfg` parameter and now branches on the raw flag AFTER `valid_num` — raw mode changes what a valid number carries (verbatim lexeme vs. `string_to_float` result), never what counts as valid, so malformed-number and token-limit behavior is identical in both modes. `build_step` converts `EvNumRaw` to `Number(Float)` on receipt via `string_to_float` (the `None` arm is unreachable for a `valid_num`-approved lexeme but keeps the match total), so `JsonStream.build` produces identical trees regardless of mode and the `Json.parse` differential holds either way. The every-byte-split differential and truncation-sweep harnesses now also run in raw-number mode (`one_shot_raw`/`two_shot_raw`/`split_all_raw`/`split_doc_raw`/`trunc_all_raw`/`trunc_doc_raw`, reusing the existing corpus lists) — the default-mode harnesses are untouched, per the phase 2 spec's "no test needing an edit to accommodate a speedup/feature" rule. **One pre-existing test helper edit** (the only one this feature needed): `ev_str` gained an `EvNumRaw(r) -> "R(" ++ r ++ ")"` arm, forced by exhaustiveness. **Deviation from the phase 2 spec's literal test 4** ("raw mode rejects malformed numbers identically"): the spec's transcribed test asserted `Err` directly out of a single `feed("012")` call, but malformed-number validation happens at the delimiter/`finish`, not synchronously during `feed` — confirmed by probing `feed("012")` in *default* mode too, which also returns `Ok` holding a pending `PNum` partial (the same deferred mechanic the pre-existing "leading zeros" test already accounts for via `one_shot`, which calls `finish`). Rewrote the test to `assert (one_shot_raw("012") == "ERR:0")`, mirroring the default-mode test one line above it. 230 → 239 tests green interpreted (`test/stdlib/test_json_stream.march`); `test/stdlib/test_json.march` unchanged at 197; `test_stdlib_march.exe test json_stream -e` green.
