# Dev tooling — spec-search Claude skill (2026-07-07)

## Closed 2026-09-09

Filed as a title-only stub with no body. The skill it names is built and
live: `.claude/skills/spec-search/{SKILL.md,spec-search.sh,spec-search.db}`
exist, `spec-search.sh "session types"` returns real indexed results, and
`git log` shows a full feature-plus-docs landing (`880a9295` feat: SKILL.md,
`304ce252` docs: README/tooling guide). Nothing left to track.
