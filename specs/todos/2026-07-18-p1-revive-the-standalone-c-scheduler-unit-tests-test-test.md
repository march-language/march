`[P1]` - [ ] **Revive the standalone C scheduler unit tests (`test/test_scheduler{,_mt}.c`).** They went DARK when the Signal.watch commit (`9c97391a`) added a `march_signal_drain()` call to `march_scheduler.c` — the standalone tests link ONLY `march_scheduler.c` (not the rest of the runtime that defines it), so they failed to LINK and `dune build` was silently red for weeks (unblocked 2026-07-18: a WEAK no-op `march_signal_drain` in `march_scheduler.c`, overridden by the runtime's strong def). Once they link, RUNNING them HANGS: `march_sched_run` now assumes the "main-as-green-thread" termination model (compiled `main` runs as a green thread whose return drives shutdown), but the tests spawn worker procs and call `march_sched_run()` from a plain C `main` with no main-green-thread, so `g_all_done` is never set and the loop idle-`nanosleep`s forever (confirmed via lldb: frame `march_sched_run` → `nanosleep`). Their RUN is disabled in `test/dune` (binaries still BUILD as a compile-check); the scheduler itself is covered by every compiled actor integration test. Revival = update the harness to the current termination contract (spawn work, then drive shutdown / mark the main proc done), then re-enable the `(alias runtest)` rules.
