# `[P2]` Compiler docs: Perceus RC + Refinement types (Core March widening slices 11 + 12, 2026-07-11)

- [ ] **Perceus/RC discipline: FBIP/reuse and atomic RC mode-selection deliberately excluded from §4.16, pending more groundwork.** `core-march.md` §4.16 states only the RC-applicability divergence (`needs_rc`/`borrow_eligible`) and the dual-position dup/drop invariant (B1) as operational rules, both already backed by a committed TIR snapshot AND now a golden. Excluded on purpose: (1) FBIP/reuse (`lib/tir/perceus_fbip.ml`) needs a "reuse preserves semantics" theorem to state as a real premise/conclusion rule — today's arity-compatibility check proves size-compatibility, not full semantic equivalence, so writing a rule would overclaim; (2) atomic RC mode-selection (`specs/atomic-rc-design.md`) is an undesigned DRAFT — no `rc_mode` field, no `lib/tir/rc_mode.ml`, no `_local`/atomic runtime variant-selection exists in the compiler today (only the always-on `march_incrc_local`/`march_decrc_local` fast path), so there is nothing implemented to document as a rule. Both are natural next widening slices once the underlying compiler work lands — not gaps in THIS slice's honesty, just explicitly out of scope.

## 2026-08-04 disposition

Reviewed as part of the refinement-types completion plan. This item is not actionable because the underlying compiler infrastructure does not exist. The todo correctly notes that `core-march.md` §4.16 deliberately excludes two topics: (1) FBIP/reuse semantics (needs a formal theorem before rules can be stated without overclaiming); (2) atomic RC mode-selection. For the latter: what does NOT exist is the general, escape-analysis-based RC-mode-selection pass designed in `specs/atomic-rc-design.md` — there is no `lib/tir/rc_mode.ml` and no `rc_mode` field. However, a narrower mechanism DOES ship: `incrc_for`/`decrc_for` in `lib/tir/perceus.ml:337-347` select `EAtomicIncRC`/`EAtomicDecRC` for any variable in `actor_sent`, with C11 atomics emitted in `llvm_emit.ml`. A future general escape-analysis pass would presumably subsume or build on this. **Unblock conditions:** either (a) the general escape-analysis RC-mode-selection pass is implemented in the compiler (and documented), or (b) an FBIP semantic-preservation theorem is committed and rules are stated in `core-march.md`. Note: `core-march.md` §4.16 carries the same overstatement ("no code implements it today") and should be corrected separately in a different task — that is a current-truth doc subject to doc-lint.
