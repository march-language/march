mod Main do
  needs IO.Console
  fn main() do
    let status = :ok
    match status do
      :ok -> println("matched ok")
      :error -> println("matched error")
      _ -> println("matched other")
    end
  end
end
