mod Main do
  needs IO.Console
  fn trace(label, v) do
    println(label)
    v
  end
  fn main(_cap_console : Cap(IO.Console)) do
    let r = { x: trace("eval-x", 1), y: trace("eval-y", 2) }
    println(int_to_string(r.x))
    println(int_to_string(r.y))
  end
end
