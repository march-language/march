mod P09BlockLetSequencing do
  needs IO.Console
  fn compute() do
    let a = 2
    let b = a * 3
    let c = b + 4
    c
  end

  fn main(_cap_console : Cap(IO.Console)) do
    println(compute())
  end
end
