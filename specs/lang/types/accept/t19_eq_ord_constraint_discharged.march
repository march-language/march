mod M do
  needs IO.Console
  fn main(_cap_console : Cap(IO.Console)) do
    let x = 3
    let y = 3
    println(if x == y do "eq" else "neq" end)
    println(if x < y do "lt" else "ge" end)
  end
end
