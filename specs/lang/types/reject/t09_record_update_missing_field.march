-- EXPECT-ERROR: This record does not have a field called `z`.
mod M do
  needs IO.Console
  fn main() do
    let p = { x: 1, y: 2 }
    let q = { p with z: 99 }
    println(int_to_string(q.x))
  end
end
