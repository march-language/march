-- EXPECT-ERROR: The linear value `s` cannot be captured by a closure
mod Main do
  needs IO.Console
  -- A local `fn ... end` is a closure too, and is called as often as you like.
  always_linear type S1 = S1(Int)
  fn sink(s : S1) : Int do match s do S1(e) -> e end end
  fn main(c : Cap(IO.Console)) do
    let s = S1(1)
    fn g() : Int do sink(s) end
    println(int_to_string(g() + g()))
  end
end
