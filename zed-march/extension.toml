id = "march"
name = "March"
version = "0.0.1"
schema_version = 1
authors = ["March Contributors"]
description = "March language support for Zed"

[grammars.march]
repository = "file:///Users/80197052/code/march"
rev = "281261a5f46c5bcbcfe12d0526b2b888e286777a"
path = "tree-sitter-march"

[language_servers.march-lsp]
name = "March LSP"
languages = ["March"]

# Debug Adapter Protocol server (`march dap`, stdio transport). The Rust
# `get_dap_binary` / `dap_request_kind` / `dap_config_to_scenario` hooks in
# `src/lib.rs` launch the `march` binary with the `dap` argument. A matching
# VS Code client lives in editors/vscode-march-debug/.
[debug_adapters.march]
# schema_path = "debug_adapter_schema.json"  # optional
