#!/bin/sh
# ufw rules for host {{host}} (pool "{{pool}}"), generated by `forge topology gen ufw`.
# Public ports open to anyone; the cluster port only to the hosts of the pools this one talks to.
set -e
{{rules}}
ufw --force enable
