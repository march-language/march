-- types (1) --
closure $Clo_$lam2$0(Ptr(Unit), Int)
-- fns (4) --
## $lam2$apply$0
fn $lam2$apply$0($clo : Ptr(Unit), x : Int) : Int =
  let n : Int = inc_rc $clo;
$clo.$fv1 in
+(x, n)

## apply_twice
fn apply_twice(f : (Int) -> Int, x : Int) : Int =
  let $t1 : Int = inc_rc f;
call_ptr f(x) in
call_ptr f($t1)

## main
fn main() : () =
  let add5 : (Int) -> Int = make_adder(5) in
let $t3 : Int = apply_twice(add5, 10) in
println($t3)

## make_adder
fn make_adder(n : Int) : (Int) -> Int =
  alloc $Clo_$lam2$0($lam2$apply$0, n)

