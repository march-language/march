mod CounterIsland do
  needs IO.Console

fn render(state_json) do
  "<span>hello from wasm</span>"
end

fn update(state_json, msg_json) do
  state_json
end

fn main(_cap_console : Cap(IO.Console)) do
  println(render("{}"))
end

end
