//! Prints the March `extern` block generated by `march::init!` — this is what
//! `forge add-rust` runs to generate the March side of the binding.
fn main() {
    demo_binding::march_print_extern_block();
}
